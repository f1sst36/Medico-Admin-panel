import { CircularProgress } from "@material-ui/core";

const App = () => {
    return (
        <div className="App">
            <CircularProgress color="primary" thickness={2} />
        </div>
    );
};

export default App;
